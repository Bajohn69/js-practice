// Element Object
/**
 * addEventListener()
 * appendChild()
 * children
 * childNode
 * classList(add(), remone(), toggle(), contains())
 *      myP.classList.add('red')
 *      myP.classList.remove('red')
 *      myP.classList.toggle('red')
 *      myP.classList.contains('red') // return boolean value
 * getAttribute()
 *      a.getAttribute('title')
 *      a.getAttribute('href')
 * innerHTML, innerText
 * parentElement
 * querySelector()
 *      // document.querySelectorAll()
 *      let redP = document.querySelectorAll('p.red')
 * 
 *      // element.object.querySelectorAll()
 *      let section = document.querySelector('section')
 *      let redP = section.querySelector('p.red')
 * querySelectorAll()
 * remove()
 * style
 *      // JS 裡面不能用減號 (-) 要改成 camelCase
 *      let button = document.querySelector('button')
 *      button.style.backgroundColor = 'black'
 *      button.style.color = 'black'
 *      button.style = 'backgroundColor: black; color: white;'
 *      // inline styling > id > class > ...
 *      button.style.color = '' //讓他的 style 歸零
 */